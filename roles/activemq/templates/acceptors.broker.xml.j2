         <acceptor name="{{ acceptor.name }}">tcp://{{ acceptor.bind_address }}:{{ acceptor.bind_port }}?{% for param in lookup('ansible.builtin.dict', acceptor.parameters | default({}), wantlist=True) %}{{ param.key }}={{ param.value }};{% endfor %}</acceptor>
