         <acceptor name="{{ acceptor.name }}">tcp://{{ acceptor.bind_address }}:{{ acceptor.bind_port }}?{% for param in lookup('ansible.builtin.dict', acceptor.parameters) %}{{ param.key }}={{ param.value }};{% endfor %}</acceptor>
